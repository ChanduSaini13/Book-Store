services:
  - type: web
    name: bookstore-backend
    env: node
    rootDir: backend
    buildCommand: npm install --include=dev && npm run prisma:generate && npm run prisma:migrate && npm run prisma:seed
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NPM_CONFIG_PRODUCTION
        value: false
